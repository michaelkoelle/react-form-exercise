import { Dancing_Script as DancingScript, Open_Sans as OpenSans } from 'next/font/google';

export const openSans = OpenSans({ subsets: ['latin'] });
export const dancingScript = DancingScript({ subsets: ['latin'] });
